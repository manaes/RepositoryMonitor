[package]
name = "gitmonitor"
version = "0.1.0"
edition = "2021"

[lib]
name = "gitmonitor"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs-next = "2"
globset = "0.4"
walkdir = "2"

[dev-dependencies]
tempfile = "3"
