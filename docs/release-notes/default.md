## RepositoryMonitor

여러 git/svn 저장소의 상태를 한 창에서 보는 데스크톱 대시보드.

### 설치
| 플랫폼 | 파일 |
|---|---|
| macOS Apple Silicon | `*_aarch64.dmg` |
| macOS Intel | `*_x64.dmg` |
| Windows | `*_x64_en-US.msi` 또는 `*_x64-setup.exe` |

> macOS 빌드는 Apple Developer ID로 서명·공증되어 별도 Gatekeeper 우회 없이 바로 실행됩니다.
